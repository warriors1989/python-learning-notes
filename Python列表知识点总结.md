# Python 列表与常用操作总结

本文用于记录 Python 列表（List）、切片操作（Slicing）以及相关的常用内置操作。后续有新的列表相关知识点将持续追加至此。

---

## 1. 列表索引与基础 (List Indexing)

Python 中的列表类似于其他语言中的“动态数组”。其索引规则如下：
* **正向索引**：从 `0` 开始。
* **反向索引**（Python 特色）：从 `-1` 开始代表最后一个元素，`-2` 代表倒数第二个，以此类推。

### 示例代码
```python
fruits = ["苹果", "香蕉", "橙子", "西瓜"]
# 正向索引:    0       1       2       3
# 反向索引:   -4      -3      -2      -1

print(fruits[0])   # 苹果
print(fruits[-1])  # 西瓜
```

---

## 2. 切片语法 (Slicing)

切片是从列表中提取子列表的强大工具。语法格式为 `list[起始索引 : 结束索引 : 步长]`。

### 核心规则
1. **左闭右开**：包含 `起始索引` 处的元素，但**不包含** `结束索引` 处的元素。
2. **省略边界**：
   * `[起:]`：省略结束索引，表示从起始索引一直截取到**最末尾**。
   * `[:止]`：省略起始索引，表示从**头部**一直截取到结束索引前。
   * `[:]`：复制整个列表。

### 常见场景与示例
```python
numbers = [10, 20, 30, 40, 50]
# 索引:     0   1   2   3   4

# 1. 截取部分：从索引 1 到 3 (不含 3)
print(numbers[1:3]) 
# 输出: [20, 30]

# 2. 从指定位置到末尾 (如 i.split('=')[1:] 常用此法提取剩余全部值)
print(numbers[1:])  
# 输出: [20, 30, 40, 50]

# 3. 从头开始截取
print(numbers[:3])  
# 输出: [10, 20, 30]
```

---

## 3. 字符串拼接 `join` 操作

### JS 与 Python 写法对比
在拼接字符串数组/列表时，Python 的写法与 JavaScript 正好相反：

* **JavaScript (方法绑定在数组上)**:
  `['foo', 'bar'].join('=')` $\rightarrow$ `"foo=bar"`
* **Python (方法绑定在连接符上)**:
  `'='.join(['foo', 'bar'])` $\rightarrow$ `"foo=bar"`

### 为什么 Python 设计成“连接符在前面”？
1. **多容器支持**：在 Python 中，`join` 可以拼接任何“可迭代对象”（如列表、元组、集合等）。如果写成列表的方法，其他容器都需要重复实现。而作为字符串的方法，字符串可以充当拼接的介质。
2. **类型安全**：`join` 仅能用于纯字符串容器。

### 常见问题与坑
如果列表中含有数字或非字符串元素，直接使用 `join` 会抛出 `TypeError`：
```python
# 错误写法 (抛出异常)
','.join([1, 2, 3]) 

# 正确写法 (使用列表推导式先将元素转为 str)
','.join([str(x) for x in [1, 2, 3]])  # "1,2,3"
```
