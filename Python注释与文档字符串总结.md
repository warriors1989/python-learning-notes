# Python 注释与文档字符串总结

在 Python 中，注释不仅用于解释代码，还用于生成自动化文档。主要分为**普通注释**和**文档字符串（Docstrings）**两大类。

## 一、 普通注释

普通注释在程序运行阶段会被 Python 解释器完全忽略。

### 1. 单行注释 (`#`)
* **语法：** 使用井号 `#` 开头。
* **规范 (PEP 8)：** 
  * 行内注释（即代码同一行的注释）与代码之间应**至少保留 2 个空格**。
  * `#` 后面应有一个空格，再书写注释内容。
  * *示例：* `x = 10  # 这是一个规范的行内注释`

### 2. 多行注释
Python 官方没有专门的多行注释语法，通常使用以下两种方式实现：
* **连续单行注释（推荐）：** 
  ```python
  # 解释第一行逻辑
  # 解释第二行逻辑
  ```
* **未赋值的多行字符串：** 使用 `'''` 或 `"""` 包裹多行文本。如果它没有赋值给任何变量，运行期间会被解释器忽略，从而起到注释作用。
  ```python
  """
  这部分内容在运行阶段会被忽略，
  可作为多行注释使用。
  """
  ```

---

## 二、 文档字符串 (Docstrings)

文档字符串是 Python 的特有机制，用于为模块、类和函数提供运行时可访问的说明文档。

### 1. 触发条件
当 `"""三双引号"""` 或 `'''三单引号'''` 字符串出现在以下位置的**第一行**时，会被识别为 Docstring：
1. **模块（文件）的最顶部**
2. **类（class）定义的第一行**
3. **函数/方法（def）定义的第一行**

### 2. 底层机制与读取方式
Docstring 会被 Python 绑定到该对象的 `__doc__` 属性上，可以通过以下方式在运行时读取：
* 使用内置函数 `help(对象)`
* 访问属性 `对象.__doc__`

---

## 三、 模块级 Docstring 的位置规范 (重要)

模块（文件）级别的文档字符串**必须写在所有 `import` 语句和可执行代码之上**，否则 Python 无法将其识别为 Docstring。

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-

"""
模块级 Docstring：必须写在这里！
用于描述本文件的功能、作者及用法。
"""

# import 语句必须在 Docstring 之后
import os
import sys
```

---

## 四、 最佳实践与规范 (PEP 8)
1. **解释 Why 而非 What：** 注释应该解释“为什么要这么做”，而不是单纯重复代码在“做什么”。
2. **同步更新：** 修改代码时，务必第一时间更新注释，避免过时注释误导他人。
