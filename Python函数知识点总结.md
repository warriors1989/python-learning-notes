# Python 函数知识点总结

## 1. 函数参数的类型提示 (Type Hints)

在 Python 3.5 中，官方引入了 **PEP 484 (Type Hints)**。它允许开发者在定义函数时，显式地声明参数和返回值的类型。

### 1.1 为什么类型提示是“可选”的？
Python 是一门**动态类型语言**，这意味着 Python 解释器在运行代码时，**不会**强制进行类型检查。即使你声明了参数类型为 `str`，但实际传入了 `int`，程序在没有报错前依然可以运行。

类型提示的主要目的是：
1. **提升可读性**：清晰地告诉阅读代码的人以及 IDE 每个参数应该接收什么类型。
2. **增强 IDE 体验**：使 PyCharm、VS Code 等编辑器能够提供更精准的代码自动补全、跳转和静态分析。
3. **静态类型检查**：配合 `mypy` 等工具，可以在不运行代码的情况下，提前发现潜在的类型不匹配错误。

---

### 1.2 为什么有些参数没有写类型提示？
当你阅读别人代码时，常会看到部分参数带有类型提示，而另一些参数没有：
1. **默认值已明示类型**：例如 `sort_type_choice=0`，由于它的默认值是 `0`，开发者往往认为它的类型（`int`）已经足够直观，因此省略了显式的 `: int`。
2. **类型较复杂**：例如 `proxies=None` 或 `geo=None`。在早期的 Python 版本中，要表示“可以是字典也可以是 None”需要导入 `Optional`（如 `proxies: Optional[dict] = None`）。为保持代码简洁或兼容性，部分开发者会选择省略。
3. **历史遗留或协作差异**：如果项目没有严格使用静态类型检查工具强制约束，不同时期的开发者或者不同的编码习惯会导致类型提示的覆盖率不一致。

---

### 1.3 类型提示的 PEP 8 风格规范

根据 **PEP 8**（Python 代码风格指南），在编写带有类型提示的函数时，空格的使用有以下严格规定：

* **仅有类型提示，无默认值时**：冒号 `:` 紧跟变量名，后面**空一格**再写类型。
  ```python
  def spider_note(self, note_url: str):
      ...
  ```
* **同时有类型提示与默认值时**：等号 `=` 的**两边都要留空格**。
  ```python
  # 规范写法
  def spider_some_note(self, excel_name: str = ''):
      ...
  ```
* **无类型提示，但有默认值时**：等号 `=` 的**两边不要留空格**。
  ```python
  # 规范写法
  def spider_some_note(self, proxies=None):
      ...
  ```

---

### 1.4 现代 Python 推荐的最佳实践 (Python 3.9+)

随着 Python 版本的迭代，类型提示的语法变得越来越简洁：

* **泛型集合 (Python 3.9+)**：不再需要从 `typing` 模块导入 `List` 或 `Dict`，可以直接使用内置的 `list` 和 `dict` 并声明内部元素类型。
  ```python
  # 推荐写法
  def spider_some_note(self, notes: list[str], base_path: dict[str, str]):
      ...
  ```
* **联合类型 (Python 3.10+)**：使用 `|` 运算符代替 `Union` 或 `Optional`。
  ```python
  # 推荐写法 (表示可以是 dict 或 None)
  def spider_some_note(self, proxies: dict | None = None):
      ...
  ```

*注：此文件后续将随着函数相关知识（如：参数传递、作用域、闭包、生成器等）学习的深入持续更新。*
