# Python 类与对象知识点总结

## 1. 对象的创建与初始化：`__new__` 与 `__init__`

虽然我们常把 `__init__` 称为“构造函数”，但从底层机制来看，Python 创建对象是由两个步骤组成的：

* **`__new__(cls, *args, **kwargs)` — 真正的构造方法 (Constructor)**：
  * **角色**：负责创建对象实例，并分配内存空间。
  * **特点**：它是一个静态方法，必须返回一个新创建 of 实例对象。
* **`__init__(self, *args, **kwargs)` — 初始化方法 (Initializer)**：
  * **角色**：负责给 `__new__` 创建出来的实例（即 `self`）进行属性配置和“精装修”。
  * **特点**：它没有返回值（必须返回 `None`）。

> **[!TIP]**
> **形象比喻**：`__new__` 负责“盖好毛坯房并交出钥匙”，而 `__init__` 负责“拿着钥匙进入毛坯房（`self`）进行精装修”。在 99% 的日常开发中，我们只需要重写 `__init__` 即可。

---

## 2. 实例方法中的 `self` 关键字

### 2.1 什么是 `self`？
* `self` 代表类被实例化后的**当前实例对象**本身（即上面 `__new__` 方法创建并分配内存后，传递给 `__init__` 那个对象）。
* 它并不是 Python 的保留关键字，而是一个约定俗成的参数名。虽然在语法上你写成 `this` 或其他名字也能正常运行，但为了代码的可读性与团队协作，**必须统一使用 `self`**。

### 2.2 为什么在定义方法时要显式接收 `self`？
Python 遵循“显式优于隐式” (Explicit is better than implicit) 的设计哲学。在类中定义普通的实例方法时，必须将 `self` 作为第一个形参显式写出来。

* **定义时**：
  ```python
  class Data_Spider:
      def spider_note(self, note_url: str):
          # self 必须作为第一个形参写出来
          ...
  ```
* **调用时**：
  ```python
  data_spider = Data_Spider()
  data_spider.spider_note("https://...") 
  # 调用时不需要手动传入 self，Python 底层会自动将 data_spider 实例作为第一个参数传入
  ```
  在底层，上述调用等价于：
  ```python
  Data_Spider.spider_note(data_spider, "https://...")
  ```

### 2.3 `self` 的核心作用
1. **绑定/访问实例属性**：
   在构造方法 `__init__` 中，可以通过 `self.属性名` 将变量绑定到实例上。
   ```python
   def __init__(self):
       self.xhs_apis = XHS_Apis() # 绑定到当前实例上
   ```
   随后在其他实例方法中，可以通过 `self.xhs_apis` 来访问这个属性。

2. **调用同类中的其他实例方法**：
   通过 `self.方法名()` 可以在一个方法内部调用该类定义的其他方法。
   ```python
   def spider_some_note(self, notes, ...):
       # 调用当前实例的 spider_note 方法
       self.spider_note(note_url, ...)
   ```

---

## 3. 属性与方法的可见性（访问控制）

与一些限制严格的语言（如 Java、C++ 等）不同，Python 没有 `public`、`private` 等硬性关键字，而是通过**命名约定**来控制可见性。

### 3.1 公开可见性 (Public)
* **特征**：默认行为，无任何特殊前缀（如 `def my_method(self):`）。
* **限制**：没有任何限制，外部和子类均可直接访问和调用。

### 3.2 保护可见性 (Protected)
* **特征**：以**单下划线 `_`** 开头（如 `def _my_method(self):`）。
* **限制**：**解释器不作硬性限制**，外部依然可以访问。这是一种“君子协定”，用来警告外部使用者“这是类内部的方法，请不要直接在外部调用”。

### 3.3 私有可见性 (Private)
* **特征**：以**双下划线 `__`** 开头，且不以双下划线结尾（如 `def __my_method(self):`）。
* **限制**：外部直接调用会触发 `AttributeError`。
* **原理（名称改编 Name Mangling）**：Python 解释器在编译时会自动将 `__方法名` 改写为 `_类名__方法名`。
  * 例如：在 `Data_Spider` 类中定义的 `def __test(self):` 实际上会被改写为 `_Data_Spider__test(self)`。如果强行用改写后的名字，在外部依然可以调用。

> **[!NOTE]**
> Python 提倡“我们都是同意的成年人” (We are all consenting adults here) 的理念。因此，它的私有机制重在“避免命名冲突”和“防止误操作”，而不是绝对的权限封锁。

---

## 4. 类中的三种方法类型对比

为了防止混淆，以下是 Python 类中常见的三种方法类型及其第一个参数的规则对比：

| 方法类型 | 装饰器 | 第一个形参 | 形参代表的内容 | 自动传入方式 | 适用场景 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **实例方法** | 无 | `self` | 当前实例对象 | 自动传入当前实例 | 需要读取或修改实例状态（最常见） |
| **类方法** | `@classmethod` | `cls` | 当前类对象 | 自动传入当前类 | 需要读取或修改类属性，或定义备用的构造方法 |
| **静态方法** | `@staticmethod` | 无 | 无特殊要求，按普通函数定义 | 不会自动传入类或实例 | 逻辑上属于该类，但完全不需要访问实例或类的数据 |

*注：此文件后续将随着面向对象（如继承、多态、魔法方法等）学习的深入持续更新。*
